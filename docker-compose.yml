services:
  # Frontend
  frontend:
    build:
      context: ./frontend
    ports:
      - "${PORT_FRONTEND}:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - VITE_GOOGLE_CLIENT_ID=${VITE_GOOGLE_CLIENT_ID}
    depends_on:
      - gateway

  # API Gateway
  gateway:
    image: nginx:alpine
    volumes:
      - ./backend/gateway/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "${PORT_GATEWAY}:80"
    depends_on:
      auth-service:
        condition: service_started
      auction-service:
        condition: service_started
      bidding-service:
        condition: service_started

  # Database
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "${PORT_DB}:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: [ "CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p${DB_ROOT_PASSWORD} --silent" ]
      interval: 5s
      timeout: 3s
      retries: 20

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: always
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
    ports:
      - "${PORT_PHPMYADMIN}:80"
    depends_on:
      - db

  # Microservices
  auth-service:
    build:
      context: ./backend/auth-service
    environment:
      - CHOKIDAR_USEPOLLING=true
      - DB_HOST=db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${DB_DATABASE}
      - PORT=${PORT_AUTH_SERVICE}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - FRONTEND_URL=${FRONTEND_URL}
      - BACKEND_URL=${BACKEND_URL}
      - AUCTION_SERVICE_URL=http://auction-service:3001
      - INTERNAL_SECRET=bidlive_secret
      - JWT_SECRET=${JWT_SECRET}
      - OPENAPI_SPEC_PATH=/openspec/specs/auth-spec.yaml
    volumes:
      - ./backend/auth-service:/app
      - /app/node_modules
      - ./openspec:/openspec:ro
      - ./backend/auth-service/uploads:/app/uploads
    # Usamos npx para evitar errores de permisos con nodemon
    command: npx nodemon index.js
    depends_on:
      db:
        condition: service_healthy

  auction-service:
    build:
      context: ./backend/auction-service
    environment:
      - CHOKIDAR_USEPOLLING=true
      - DB_HOST=db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${DB_DATABASE}
      - PORT=${PORT_AUCTION_SERVICE}
      - SERVICE_PREFIX=/auction
      - AUTH_SERVICE_URL=http://auth-service:3000
      - INTERNAL_SECRET=bidlive_secret
      - JWT_SECRET=${JWT_SECRET}
      - CHAT_SERVICE_URL=http://chat-service:3004
      - OPENAPI_SPEC_PATH=/openspec/specs/auction-spec.yaml
    volumes:
      - ./backend/auction-service:/app
      - /app/node_modules
      - ./openspec:/openspec:ro
      - ./backend/auction-service/uploads:/app/uploads
    command: npx nodemon index.js
    depends_on:
      db:
        condition: service_healthy

  bidding-service:
    build:
      context: ./backend/bidding-service
    environment:
      - CHOKIDAR_USEPOLLING=true
      - PORT=${PORT_BIDDING_SERVICE}
      - DB_HOST=db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${DB_DATABASE}
      - JWT_SECRET=${JWT_SECRET}
      - AUTH_SERVICE_URL=http://auth-service:3000
      - INTERNAL_SECRET=bidlive_secret
    volumes:
      - ./backend/bidding-service:/app
      - /app/node_modules
      - ./openspec:/openspec:ro
    command: npx nodemon index.js
    depends_on:
      db:
        condition: service_healthy

  chat-service:
    build:
      context: ./backend/chat-service
    environment:
      - CHOKIDAR_USEPOLLING=true
      - DB_HOST=db
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASSWORD}
      - DB_NAME=${DB_DATABASE}
      - PORT=${PORT_CHAT_SERVICE}
      - INTERNAL_SECRET=bidlive_secret
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./backend/chat-service:/app
      - /app/node_modules
    command: npx nodemon index.js
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_data:
