# Copia este archivo como .env.production en tu servidor
# y rellena los valores reales. NUNCA lo subas a GitHub con valores reales.

# Database
DB_ROOT_PASSWORD=cambia_esto_por_una_password_segura
DB_DATABASE=bidlive
DB_USER=bidlive_user
DB_PASSWORD=cambia_esto_por_una_password_segura

# Ports
PORT_FRONTEND=3000
PORT_GATEWAY=8080
PORT_AUTH_SERVICE=3000
PORT_AUCTION_SERVICE=3001
PORT_BIDDING_SERVICE=3002

# URL publica del gateway (tu IP o dominio)
VITE_SIGNALING_URL=http://TU_IP_O_DOMINIO:8080
